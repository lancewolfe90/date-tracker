import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'birthday-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
